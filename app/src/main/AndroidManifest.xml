<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.nazycodes.foodiex">

    <dist:module dist:instant="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/foodie_black"
        android:label="@string/app_name"
        android:roundIcon="@drawable/foodie_black"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".screens.OrderScreen"
            android:theme="@style/AppTheme.FullScreen"></activity>
        <activity
            android:name=".screens.FoodDetailsScreen"
            android:theme="@style/AppTheme.FullScreen"/>
        <activity
            android:name=".screens.SignupScreen"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".screens.LoginScreen"
            android:theme="@style/AppTheme.FullScreen" />
        <activity
            android:name=".screens.SplashScreen"
            android:theme="@style/AppTheme.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.FullScreen" />
    </application>

</manifest>